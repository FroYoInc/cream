import models = require('../models/models');

class Carpool {
  private _name : string;
  private _users : [models.User];
  private _id : string;
  private _campus: Campus;
  private _description: string;
}
